# FDvault workspace

- Use React and TypeScript conventions already present in the Vite project.
- Keep deposit data local-first unless a backend is explicitly requested.
- Validate changes with `npm run build` and `npm run lint`.
- Keep financial calculations transparent and editable from the deposit form.
